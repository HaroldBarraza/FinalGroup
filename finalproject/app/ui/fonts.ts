import { Inter, Playfair_Display, Roboto, Quicksand } from 'next/font/google';


export const inter = Inter({ subsets: ['latin'] });
export const playfairDisplay = Playfair_Display({ subsets: ['latin'] });
export const roboto = Roboto({ subsets: ['latin'] });
export const quicksand = Quicksand({ subsets: ['latin'] });

